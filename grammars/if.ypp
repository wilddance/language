%%
%%

if: if_bool block END_ST{

    lines.unnest();

    int jump_line = lines.code_size() + help - lines.nestLvl;
    char* a = str("@%d", jump_line);

    jlz(a);

    lines.copy_buffer();
    help--;
}

// odpowiada za pushe i jump
if_bool: if bool_exp{
    lines.nest();
}

if: IF {
    help++;
}
